{
  "name": "single-spa",
  "version": "1.0.0",
  "description": "the minimum implement of single_spa",
  "main": "dist/index.cjs",
  "types": "dist/index.d.ts",
  "module": "dist/index.mjs",
  "scripts": {
    "build": "tsup --config tsup.config.ts",
    "watch": "tsup --config tsup.config.ts --watch",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "tsup": "^6.2.3",
    "typescript": "^4.8.2"
  }
}
